{
  "name": "postman-automation",
  "version": "1.0.0",
  "description": "Automation of Postman collection tests",
  "scripts": {
    "test": "newman run Collections/BrandAPI.postman_collection.json --environment Environments/BrandAPI.postman_environment.json -r htmlextra --reporter-htmlextra-export reports/newman-report.html"
  },
  
  "dependencies": {
    "newman": "^6.2.1",
	"newman-reporter-htmlextra": "^1.23.1"
  }
}
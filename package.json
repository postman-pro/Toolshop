{
  "name": "postman-automation",
  "version": "1.0.0",
  "description": "Automation of Postman collection tests",
  "scripts": {
    "test": "newman run collection/BrandAPI_POST_SmokeTests.postman_collection.json --environment environment/ToolshopTEST.postman_environment.json -r cli,htmlextra --reporter-htmlextra-export post-report.html | tee newman.log"
  },
  
  "dependencies": {
    "newman": "^6.2.1",
	"newman-reporter-htmlextra": "^1.23.1"
  }
}